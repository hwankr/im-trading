# 운영 전환 이력

## 고정 진입가·청산가 방식 · 2026-09-10

전환 전 커밋: `6906759f09c64f73fd8c63f0ef81355132bd8ebe`.
백업 브랜치: `archive/pre-fixed-price-20260910`.

현재 절차는 `prompts/plan.md`다. 장전·장중·장후 모두 사용자 요청으로 한 번 분석해
진입가 1개·익절가 1개·손절가 1개와 기한을 확정한다. 기존의 **장전만 발행** 제한은 폐기한다.
기존 `prompts/premarket.md` 경로는 공통 절차로 연결하므로 이전 요청 문구도 사용할 수 있다.
계좌 미확인만으로 가격표를 막지 않고 PRICE_ONLY로 제공한다. 수량과 주문 실행 가능 여부는 별도로 확인한다.

이 변경은 지침·설정·양식의 전환이다. 시세 수집기·자동 주문·실시간 감시를 구현하거나 실행한 것이 아니다.
대회 설정과 원본 PDF, 계좌 초기 양식, 비공개 경로 제외 규칙은 보존한다. 대회 규정을 이번에 재검증했다는 의미가 아니다.
개별 확정 계획은 자동으로 새 전략으로 바꾸지 않는다. 새 요청에서 기존 주문·보유를 확인하고 새 버전의 대체 관계를 남긴다.

Git 백업에는 비공개 `data/`·`runs/`가 포함되지 않는다. 기존 비공개 기록은 별도로 보존한다.
외부 예약 상태를 조회·중지하거나 앱 주문을 취소한 작업이 아니므로, 남아 있는 예약·주문은 별도로 확인해야 한다.
기존 기록을 강제 초기화·삭제하는 명령은 사용하지 않는다.

## 이전 이력: 장전 운영으로 전환 · 2026-09-08

아래는 당시 전환 이력이다. 현재 운영 지침이 아니며, 당시의 장전 전용 제한은 위 2026-09-10 전환으로 대체됐다.

### 보존한 것

정리 직전 커밋: `ce04201601797d95e3524c99b3beeb824fb1c2c3`.
백업 브랜치: `archive/pre-premarket-20260908`.
이 브랜치에 기존 블로그, 장중 운영 지침, 설계·인계 문서, 설정을 원형 보존했다.
작업 브랜치에는 같은 자료를 archive 폴더로 복제하지 않는다. 과거 자료가 필요할 때만 백업 브랜치에서 읽는다.
원본 매뉴얼 PDF는 내용과 경로를 그대로 유지했다.

정리 전 저장소에는 실행 코드가 없었다. 당시 변경은 문서·설정·프롬프트·기록 양식을 정리한 것이며,
시세 수집기나 매매 엔진을 제거·구현·테스트한 작업이 아니었다.

### 이전 계좌 기록을 현재값으로 쓰지 않기

이전 README는 2026-09-08 12:20:06 KST에 첫 주문이 전량 미체결 취소되어 체결 0주·잔량 0주,
활성 지시 0건이었다고 기록한다. 현금 1억 원 및 주문가능 53주는 11:56의 과거 보고이며
취소 후 값으로 갱신하지 않았다고 명시한다. 이는 이전 문서의 기록이지 이번 작업에서 확인한 현재 계좌가 아니다.

비공개 원본은 기존 `runs/im-rookie-2026-paper-main/`에 있으며 Git 추적 대상이 아니었다.
GitHub만으로 이후 체결·잔고를 검증할 수 없으므로 새 계좌 파일은 UNVERIFIED/null에서 시작한다.
이것은 계좌 초기화나 기록 삭제가 아니다. 기존 runs를 보존하고 최신 앱 보고와 대사해 요약만 옮긴다.
공식 수익률·회전율·거래일수·인정 종목 수를 블로그에서 추정해 복원하지 않는다.

### 당시 전환 순서와 외부 확인 항목

1. 기존 PC의 비공개 runs·계좌 자료를 별도 백업한다. Git 브랜치 백업은 이 자료를 포함하지 않는다.
2. 저장소 밖에 등록됐던 **AI 모의투자 장중 시장 점검** 예약을 비활성화하고 실행 중인 작업도 확인한다.
   당시 정리와 이번 전환 모두 외부 예약 상태를 조회하거나 중지한 작업이 아니다. 문서 변경만으로 예약은 멈추지 않는다.
3. 앱에서 전체 계좌·미체결·최근 체결/취소를 확인한다. 이미 접수된 주문은 문서 변경이나 지시 만료로 취소되지 않는다.
4. `templates/account.json`으로 만든 로컬 요약에 확인된 사실만 옮기고 출처·시각을 남긴다.
5. 당시에는 다음 거래일 장전부터 사용하도록 했다. 이 장전 전용 운영 제한은 현재 폐기됐으며 `prompts/plan.md`를 따른다.

이전 방식이 필요하면 `git fetch origin` 후 백업 브랜치를 별도로 확인한다.
기존 로컬 기록을 자동 복원·삭제하거나 현재 작업 파일을 강제로 초기화하는 명령은 사용하지 않는다.
