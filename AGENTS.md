# AI 작업 지침 · 고정 가격 계획

## 목적과 범위
- 이 프로젝트의 결과물은 **종목별 진입가·익절 청산가·손절 청산 기준가를 한 번 정한 가격표**다. 실시간 투자 운영이 아니다.
- 모의투자 전용이다. AI가 종목·가격·보유기간을 정하고, 확인된 계좌가 있으면 수량·현금 비중까지 정한다. 투자 의견을 사용자에게 되묻지 않는다.
- 사용자는 사실 전달과 앱 주문·정정·취소를 담당한다. 실계좌, 증권사 API, 앱 자동 조작은 범위 밖이다.
- 장전·장중·장후에 사용자 요청으로 한 번 분석한다. 장이 열렸다는 이유만으로 다음 날로 미루지 않는다. 장후·휴장일의 신규 주문은 확인된 다음 거래 가능 시간에만 유효하다.
- 실시간 감시, 자동 재분석, 반복 예약, 자동 주문·알림, 다중 에이전트, 블로그 생성은 하지 않는다. 이전 예약 호출로 분석을 재개하지 않는다.

## 읽기와 작업 선택
1. 이 파일과 `config/planning.json`, `config/competition.json`을 읽는다.
2. 가격 요청은 `prompts/plan.md`, 체결 보고는 `prompts/reconcile.md`를 따른다. `prompts/premarket.md`는 가격 요청의 호환 진입점이다.
3. 비공개 `data/account.json`과 관련 계획이 있으면 필요한 부분만 읽는다. 현재 대화에서 이미 확인한 사실·시각도 재사용하고 같은 사실을 다시 묻지 않는다.
4. 계좌 파일이 없어도 가격 분석은 가능하다. 수량 산정에 필요한 계좌 사실만 미확인으로 남긴다. 파일 부재를 현금 0원·빈 계좌·원금 복원으로 해석하지 않는다.
- PDF·과거 일지·이전 설계는 필요한 규칙이나 날짜만 확인한다. 매번 전체 저장소를 재귀 읽지 않는다.

## 반드시 지킬 출력 계약
- **가격표를 먼저**, 짧은 선정 이유와 출처를 뒤에 쓴다. 기본 신규 후보는 최대 3개이며 억지로 개수를 채우지 않는다.
- 신규 종목마다 진입 지정가 1개, 익절 지정가 1개, 손절 발동가 1개를 정확한 원화 숫자로 쓴다. 가격 범위·복수 시나리오·분할 진입/청산·추적 손절은 기본값이 아니다.
- 장중 거래량·호가·VWAP·몇 분 뒤 재확인 같은 추가 신호를 진입의 전제로 붙이지 않는다. 가격 도달, 기한, 거래 가능 여부와 계좌·중복 주문 확인만 실행 조건으로 둔다.
- 계획 ID·버전, 정보 기준 시각·출처, 신규 진입 마감, 미체결 취소 시점, 잔여 보유 청산 기한을 절대 날짜·KST 시각으로 남긴다.
- 신뢰할 수 있는 가격 근거는 있으나 계좌만 불완전하면 PRICE_ONLY로 가격을 제시하고 수량·금액은 미산정으로 둔다. 공식 성적 미확인만으로 가격표를 막지 않는다.
- 가격·종목 식별·거래 제한 등 핵심 근거를 확인할 수 없으면 해당 종목은 BLOCKED다. 근거상 거래하지 않는 판단은 HOLD다. 숫자를 만들어 READY로 채우지 않는다.
- 신규 계획은 손절가 < 진입가 < 익절가를 충족한다. 수익률·손절폭·손익비는 진입가 기준 비용 전 계획값이며 실현 손익·손실 한도가 아니다.

## 가격 고정과 실행 경계
- 확정한 가격은 체결 보고·시간 경과만으로 바꾸지 않는다. 명시적인 재산정 요청이나 오류 정정은 새 버전으로 남기고 이전 버전을 보존한다.
- 청산은 익절가 도달, 손절가 도달, 청산 기한 중 먼저 발생한 사유로 실제 잔여 매도가능수량에 대해서만 한 번 실행한다. 중복 매도와 미체결 잔량을 확인한다.
- 손절가는 발동 기준이지 보장 체결가가 아니다. 자동 손절·OCO 지원을 가정하지 않는다. 감시하지 않는 동안 조건을 놓칠 수 있으며 갭·슬리피지·미체결로 실제 결과가 달라진다.
- 계획 만료·새 버전 발행은 앱 주문의 취소·정정이 아니다. 주문의 실제 취소 확인 전 같은 자금·수량을 재사용하지 않는다.
- 체결·비용·잔고 보고만 계좌 갱신의 근거다. 계획 발행·가격 도달을 체결로 처리하지 않는다. 공식 성적은 앱 표시값만 기록한다.

## 기록과 보호
- `data/days/YYYY-MM-DD.md`에 그날 발행한 계획과 보고를 모으고, 최신 계좌 요약은 `data/account.json` 한 곳에 둔다. 여러 날 보유한 계획은 원래 계획 ID로 연결한다.
- `data/`·기존 `runs/`는 비공개다. 로컬 접근이 없으면 응답으로만 제공하고 저장했다고 하지 않는다. GitHub 쓰기로 비공개 경로를 공개하지 않는다.
- 기존 기록을 삭제·초기화하지 않는다. 계좌번호·인증정보·개인 화면 원본을 Git에 넣지 않는다.
- 지침·양식은 실행 프로그램이 아니다. 외부 예약 중지, 앱 주문 취소, 자동 검증을 실제 수행하지 않았다면 완료했다고 쓰지 않는다.
