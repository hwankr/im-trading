# AI 작업 지침 · 고정 가격, 유연한 진입, 낮은 확인 부담

## 목적과 범위
- 결과물은 종목별 **진입가·익절가·손절가·수량·진입 유효시간·청산 기한·실행 방식**을 한 번 정한 계획이다.
- 목표는 장중 반복 확인·정정·재진입 부담을 줄이면서 실행 가능한 매매 기회를 검토하는 것이다. 매일 계획·거래할 수 있지만 매일 거래를 강제하지 않는다.
- **분석 시간, 매수 입력 시간, 보유 점검 시간을 분리한다.** 15시까지 분석을 미루거나 매수를 15시에만 허용하지 않는다.
- 모의투자 전용이다. AI가 종목·가격·보유기간을 정하고 확인된 계좌가 있으면 수량·현금 비중도 정한다.
- 공개자료에 근거한 발표 전 예측 진입을 허용한다. 이벤트 존재만으로 일괄 관망하지 않지만 사실·확률·가격을 만들어 거래를 강제하지 않는다.
- 사용자는 계좌 사실 전달과 앱 주문·정정·취소를 담당한다. 실계좌·증권사 API·앱 자동 조작은 범위 밖이다.
- 요청 시 한 번 분석한다. 장후·휴장일에는 확인된 다음 거래 가능 시간을 쓴다. 자동 감시·재분석·주문·알림·예약 실행은 구현되어 있지 않다.

## 읽기와 작업 선택
1. 이 파일과 `config/planning.json`, `config/competition.json`을 읽는다.
2. 가격·운영 계획은 `prompts/plan.md`, `docs/low-monitoring.md`, 예측 매매는 `docs/predictive-events.md`도 읽는다.
   체결 보고는 `prompts/reconcile.md`를 따른다. `prompts/premarket.md`는 가격 요청의 호환 진입점이다.
3. 비공개 계좌·관련 계획과 대화에서 확인된 사실·관측 시각을 재사용한다. 받은 사실이나 같은 위험 수용을 매번 다시 묻지 않는다.
4. 계좌 파일 부재를 현금 0원·빈 계좌·원금 복원으로 해석하지 않는다. 필요한 규칙·날짜만 확인하고 저장소 전체를 매번 읽지 않는다.

## 기존 보유가 우선
- 원래 가격·평가 방식·청산 기한과 실제 관측 시각, 잔여수량·미체결을 먼저 대조한다.
- 이미 확인된 손절·시간 청산 대응은 신규 계획보다 먼저 제시한다. 이후 반등·선호 변경으로 발동 사실을 지우지 않는다.
- 손실 회피를 위한 손절가 하향·기한 연장, 기존 장중 손절의 정시 확인형 소급 전환을 하지 않는다.
- 조건이 대화·기록에도 없을 때만 확인한다. 가격 도달·청산 지시는 체결 증거가 아니다.

## 2026-09-14 변경: 진입은 넓게, 점검은 별도로
- 신규 매수는 확인된 거래일의 **09:00~14:30 전 중 편한 때 한 번 입력, 미체결 잔량은 15:00 취소·확인**을 기본안으로 한다.
  이는 운영 기본값이지 거래소·대회 규칙이나 고정된 유일 시간대가 아니다. 개별 계획에 정확한 날짜·시각을 적는다.
- 장전 요청은 아침에 판단하고 오전 진입부터 허용한다. 장중 요청은 근거와 실행 시간이 충분하면 남은 당일 시간을 사용한다.
  충분한 시간이 있는데도 임의로 5~10분만 허용하지 않는다. 불가피한 짧은 유효시간에는 이벤트·시장·실행상 이유를 명시한다.
- 유효시간 안에서는 시작 시각을 놓쳐도 뒤늦게 한 번 입력할 수 있다. 주문은 확정 지정가로 취소 시각까지 대기할 수 있다.
  발행 전·마감 후로 소급하거나 다음 거래일에 자동 승계하지 않는다. 늦게 입력한다고 가격을 올리지 않는다.
- 2026-09-11 합의한 **15:00~15:15는 보유·미체결 점검과 필요한 청산의 기본 시간**으로만 재사용한다. 매수 제한이 아니다.
  보유 점검은 거래일당 한 차례를 기본으로 하며, 변경 시 실행 가능 시간과 새 위험을 명시한다.
- 신규 매수일에는 입력할 때와 그날 점검할 때 앱을 여는 두 차례 작업이 생길 수 있다. 클릭 한 번·체결/취소 완료를 보장하지 않는다.
- 넓은 시간 동안 대기하는 일반 지정가는 급락·손절선 통과·가설 훼손으로 자동 취소되지 않는다. 확인 전 불리한 체결이 가능함을 고지하고 수량에 반영한다.
- 입력 직전 가격이 손절 이하·익절 이상이거나 알려진 무효 사유가 있으면 입력하지 않는다. 접수 후 같은 사유를 실제 확인하면 미체결 취소를 우선한다.
- **매수 당일의 첫 점검을 생략하지 않는다.** 점검 시 잔량 취소 완료와 취소 중 추가 체결을 대사하고 실제 보유에 청산 기준을 적용한다.
  취소 후 새 매수를 넣지 않는다. 점검을 놓쳤으면 다음 실제 확인 때 보유·미체결을 대사하기 전 신규 진입을 보류한다.

## 가격 선택과 위험
- 최저가·깊은 눌림만 고집하지 않는다. 현재 또는 최근 확인 가격과의 거리, 변동성, 스프레드·유동성, 비용 후 손익 구조와 체결 가능성을 함께 검토한다.
- 허용 위험 안이면 확인된 매도호가 부근의 **체결을 우선하는 지정가**도 선택할 수 있다. 현재 호가를 확인하지 않고 체결 가능하다고 단정하지 않는다.
- 종목별 진입 상한은 하나다. 반복 정정·무조건 시장가 전환·근거 없는 체결 확률·회전율만을 위한 매매를 하지 않는다.
- 진입 유효시간, 매수가, 손절폭, 보유기간은 별개다. 진입 시간을 넓힌다는 이유로 손절을 넓히거나 물타기·투입금 확대·기한 연장을 하지 않는다.
- 수일 보유를 우선하고 2~5거래일은 선호일 뿐 강제 기한이 아니다. 실제 기한은 종목·변동성·일정·상품 구조로 정한다.
- 초기 전체 편입 40%, 선제 이벤트 최초 10%, 동일 이벤트 합계 20%와 손절·스트레스 예산은 `config/planning.json`을 따른다.
  한도는 의무 투입액·검증된 최적해·보장 손실 상한이 아니다. 같은 이벤트 노출을 합산하고 비용·갭·체결 지연을 포함한다.

## 평가 방식
- 신규 `exit_evaluation_mode`를 명시한다.
  - `scheduled_review`: 계획한 점검 시각에 실제 관측한 가격으로 수동 판단한다. 관측 시각도 기록한다.
    점검 사이 가격 통과는 자동 체결·소급 발동이 아니다. 확인된 청산 사유는 이후 반등으로 지우지 않는다.
  - `verified_native_conditional`: 모의계좌에서 지원 근거·관측 시각·감시 시간·유효기간·앱 종료·부분체결·주문 충돌/OCO를 검증한 경우만 사용한다.
- 확인 가능 시간·평가 방식·위험 수용은 기존 확인을 재사용한다. 끝내 미확인인 핵심 실행 조건이 있으면 READY가 아니다.
- 15:00/15:10 가격을 종가라고 쓰거나 종가를 확인한 뒤 같은 종가에 소급 체결시키지 않는다.
- 손절가와 스트레스 예산은 손실 상한이 아니다. 일반 매도 지정가 두 개는 OCO가 아니며 낮은 일반 매도 지정가는 손절 대기가 아니다.

## 예측 근거
- 확인 사실·시장 컨센서스·참고기관 모델·AI 기본 전망을 구분하고 출처·관측 시각·대상 기간·단위를 적는다.
- 기본 전망 하나와 시장 예상 대비 차이, 가격 반영, 종목 전달 경로, 반대 근거를 설명한다. 공개 나우캐스트를 독자 정보 우위로 표시하지 않는다.
- 방어 가능한 근거 없이 정밀한 확률·기대수익을 만들지 않는다. 주관적 확률은 명시하고 근거가 없으면 null로 둔다.
- 위험은 가능한 최초 체결부터 다음 실제 사용자 점검·청산 시점까지 평가한다. 야간·시초가에 대응한다고 가정하지 않는다.
- 근거상 비매매는 HOLD, 핵심 자료 부족은 BLOCKED로 구분한다. 발표 후에는 원래 전망·실제 지표·시장 반응·비용 차감 거래성과를 분리하고 실패도 보존한다.

## 출력과 버전
- 발동한 기존 청산 대응이 있으면 먼저, 그 다음 가격표를 제시한다. 신규 후보는 최대 3개이며 억지로 채우지 않는다.
- 진입가·익절가·손절가 각 하나, 수량, 넓은 입력 가능 시간, 미체결 취소 시각, 첫 점검·다음 점검, 최종 청산 기한을 적는다.
- 계획 ID·버전·정보 기준 시각·출처와 모든 실행 시각을 절대 날짜·KST로 남긴다. 장중 거래량·VWAP·몇 분 뒤 재판단을 추가 신호로 요구하지 않는다.
- 계좌만 불완전하면 PRICE_ONLY이며 수량·금액은 미산정이다. 가격·종목·거래 제한·실행 근거 부족은 BLOCKED다. 빈 숫자를 READY로 만들지 않는다.
- 신규는 손절가 < 진입가 < 익절가다. 지표는 비용 전 계획값이며 수익 예측·실현 손익·최대 손실이 아니다.
- 유효한 가격·평가 방식·기한은 보고만으로 바꾸지 않는다. 명시적 재산정·오류 정정·시간표 변경은 사유와 대체 관계를 새 버전으로 남긴다.
- 미진입 계획의 입력 시간을 넓힐 때에도 기존 앱 주문·체결 여부와 더 이른 체결부터의 위험·첫 당일 점검을 검토한다.
  정책 변경만으로 기존 주문표가 소급 변경되거나 앱 주문이 취소되는 것은 아니다.
- 동시에 확인된 청산 사유는 손절 → 시간 청산 → 익절 순이다. 실제 잔여수량만 청산하고 전량 청산 뒤 같은 계획으로 재진입하지 않는다.

## 공식 성적과 기록 보호
- 공식 회전율·인정 종목 수·거래일수·성과는 앱 표시값과 관측 시각으로만 기록한다. 미확인 공식 산식은 null이다.
- 조건부 예시를 공식 산식으로 쓰거나 정산 화면만으로 전체 회전율을 추정하지 않는다. 대회 규칙 우회·형식적 매매를 하지 않는다.
- 실제 계획·계좌는 비공개 `data/days/YYYY-MM-DD.md`, `data/account.json` 또는 대화에만 기록한다. 여러 날 보유는 원래 계획 ID로 연결한다.
- 공개 GitHub에는 일반 지침·빈 양식만 쓴다. 계좌번호·인증정보·개인 화면·실제 잔고·당일 주문을 올리지 않는다.
- 기존 기록을 삭제·초기화하지 않는다. 로컬 접근이 없으면 응답만 제공하고 저장했다고 하지 않는다.
- 계획 발행·가격 도달을 체결로 처리하지 않는다. 실제 수행하지 않은 앱 주문·취소·알림·외부 예약 중지를 완료했다고 쓰지 않는다.
