# AI 작업 지침

## 범위
- 모의투자 전용. 종목·진입가·손절가·익절가·수량·현금 비중은 AI가 결정한다. 사용자에게 투자 의견을 묻지 않는다.
- 사용자는 계좌 사실 전달과 앱 주문·정정·취소만 담당한다. 실계좌, 증권사 API, 앱 자동 조작은 범위 밖이다.
- 운영은 **사용자 요청에 따른 장전 계획 1회 → 사용자의 조건부 실행 → 체결 보고 대사**다.
- 실시간 시세 감시, 장중 재분석, 반복 예약, 자동 주문·알림, 다중 에이전트, 작업별 블로그 생성은 사용하지 않는다.
- 이전 예약 호출이 들어와도 시장 조회나 투자 지시를 재개하지 않는다. 사용자에게 외부 예약 중지 필요만 안내한다.

## 최소 읽기
1. 이 파일, `config/competition.json`, `data/account.json`을 읽는다. 계좌 파일이 없으면 최초 계좌 보고부터 받는다.
2. 장전 판단은 `prompts/premarket.md`, 체결 반영은 `prompts/reconcile.md`와 해당 날짜 기록만 추가로 읽는다.
3. `README.md`는 설치·사용법, `docs/rules.md`와 PDF는 규칙 확인, `docs/migration.md`는 이전 작업 때만 읽는다.
- 저장소 전체 재귀 읽기, 과거 기록 전수 로딩, 매번 PDF 재추출을 하지 않는다. 과거 자료는 필요한 날짜·항목만 연다.

## 판단과 기록
- 시장 자료는 직접 확인한 출처·관측 시각·판단 기준 시각을 남긴다. 전일 종가를 현재가라고 쓰지 않는다.
- 핵심 자료가 없으면 BLOCKED, 거래하지 않기로 판단하면 HOLD. 모르는 값은 null이며 0이나 빈 계좌로 추정하지 않는다.
- 계획에는 실행 가능한 숫자·조건·유효일·입력 기한·미체결 취소 조건을 쓴다. 입력 기한 경과는 앱 주문 취소가 아니다.
- 손절·익절은 사용자가 실행할 조건이지 자동 주문이나 보장 가격이 아니다. 갭·미체결·부분 체결을 구분한다.
- 계획 작성만으로 잔고를 바꾸지 않는다. 실제 모의계좌 체결·비용·잔고 보고가 자산 반영의 근거다.
- 공식 수익률·회전율·인정 종목 수·거래일수는 공식 표시값만 기록한다. 계산 추정치를 공식값으로 대체하지 않는다.
- 날짜별 `data/days/YYYY-MM-DD.md` 하나에 계획과 보고를 모은다. 확정 계획은 보존하고 보고·정정·사후 해석은 뒤에 추가한다.
- 최신 계좌 요약은 `data/account.json` 한 곳에만 둔다. README·규칙 설정·블로그에 현재 상태를 복제하지 않는다.
- 사용자 사실 보고와 투자 의견 개입을 구별한다. 개입이 있으면 기록하고 무개입 실험으로 표시하지 않는다.

## 보호
- `data/`와 기존 `runs/`는 비공개다. 계좌번호·인증정보·개인 화면 원본을 Git에 넣지 않는다.
- 기존 `runs/`를 삭제·초기화하지 않는다. 백업 브랜치는 Git 추적 파일만 보존하며 비공개 기록은 포함하지 않는다.
- 외부 예약을 실제 중지하지 않았다면 중지 완료라고 쓰지 않는다. 문서·양식을 실행 프로그램이나 자동 검증으로 표현하지 않는다.
